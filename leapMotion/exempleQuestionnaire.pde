  Questionnaire q;  
  
String [] Texte;

/*
  void setup(){
    Texte = loadStrings("Score.txt");
    q = new Questionnaire(0);
    q.question1(true);
    println("Vous avez fait un score de "+q.score+" erreurs /"+q.maxScore);
    q.getClassement();
    println("les autres ont fait : ");
    for(int i=0; i< Texte.length;i++) println( Texte[i]+" erreurs /20");
    q.stockScore();
  }
  void draw(){
    
  }
  
  */
