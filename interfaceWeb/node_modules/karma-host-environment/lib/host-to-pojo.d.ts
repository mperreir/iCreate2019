declare type POJO = Record<string, unknown>;
/**
 * Returns a copy of the `host` object as a POJO (plain old JavaScript object)
 * that's safe to serialize as JSON.
 */
export declare function hostToPOJO(): POJO;
export {};
